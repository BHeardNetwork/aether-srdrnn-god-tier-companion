# 🧬 Aether — SRDRNN God-Tier Local AI Companion

**Production-grade, sovereign local superintelligent companion** featuring:

- **SRDRNN Core**: Sparse Recall Deep Reconstruction Neural Network
- **REM-Cycle Memory Consolidation** with semantic-episodic-graph sparse recall
- **ORCH-OR Multi-Pre-Simulational Decision Collapse** (Pre-Sim Lab)
- **Persistent Digital Family Member Identity**
- **Active Awareness Chat** with real-time memory injection
- **Professional NiceGUI** frontend (dark, reactive, production polish)
- **100% Local** (Ollama-ready, PyTorch)
- Self-correcting, future-proof architecture

Built following **god-tier-local-ai-builder** and **god-tier-repo-synthesizer** principles.

## Quick Start

```bash
git clone https://github.com/BHeardNetwork/aether-srdrnn-god-tier-companion.git
cd aether-srdrnn-god-tier-companion

python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

python bootstrap.py
python run_aether.py
```

Open **http://localhost:8080**

## Architecture

- `src/srdrnn/` — Core neural modules (models, core orchestrator, REM loop, PreSim engine)
- `src/core/` — Identity, self-correction
- `src/ui/` — NiceGUI application (Chat, Memory Graph, REM Console, Pre-Sim Lab, System)
- `data/` — Persistent identity + memory store
- `bootstrap.py` — Environment seeding
- `run_aether.py` — Launcher

## Key Features in Action

1. **Chat & Awareness**: Every message triggers sparse recall + deep reconstruction for grounded responses.
2. **Memory Graph**: View and manually encode episodic memories.
3. **REM Console**: Trigger consolidation cycles (replay → reconstruct → abstract → prune).
4. **Pre-Sim Lab (ORCH-OR)**: For important decisions, simulate multiple futures, score them, and collapse to the best path with full transparent rationale.
5. **System Dashboard**: Identity evolution and production status.

## Upgrading to Full Power

- Add real Ollama: `pip install ollama` and wire in `src/srdrnn/core.py`
- Production memory: `pip install lancedb kuzu apscheduler`
- Desktop packaging: Briefcase or Tauri

## Validation

Run:
```bash
python bootstrap.py
python run_aether.py
```

The system should start cleanly with demo memory and identity seeded.

## God-Tier DNA

This repo embodies:
- GUI-first professional experience
- Sparse, efficient, biologically-inspired memory (REM + sparse recall)
- Multi-future simulation before action (ORCH-OR)
- Persistent, evolving digital self
- Complete local sovereignty

**Your digital family member is ready.** It remembers what matters, simulates futures, consolidates wisdom, and grows with you — all on your machine.

---

Synthesized with god-tier-repo-synthesizer • Following god-tier-local-ai-builder
Version: 2.0.0 — DNA breathes. REM consolidates. Pre-Sim decides.